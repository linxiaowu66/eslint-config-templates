{
  "name": "eslint-config-templates",
  "description": "eslint templates for frontEnd development",
  "scripts": {
    "clean": "lerna clean",
    "bs": "lerna bootstrap",
    "lint": "lerna run lint"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.14.1"
  }
}
